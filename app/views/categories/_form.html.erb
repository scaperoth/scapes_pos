<%= simple_form_for [current_team, @category] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :price %>
    <%= f.input :regex %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
